#ifndef PRINTER_H
#define PRINTER_H

/* class for printing messages. */
class printer {
  public:
    /* prints the help message. */
    static void help(char*);
    
    /* prints version info. */
    static void version();
};

#endif
